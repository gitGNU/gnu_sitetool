2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* NEWS:
	* test/site/test.in:

	Updated

2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* configure.ac.in:
	* libs/perl/Sitetool/Filterchain.pm:
	* libs/perl/Sitetool/Filters/Makefile.am:
	* libs/perl/Sitetool/Filters/Skeleton.pm:
	* libs/perl/Sitetool/Makefile.am:
	* src/filters/wikitext-sxml.in:

	Removed Filters perl library (useless at the moment)

2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* Makefile.sub:
	* src/sitetool-build.in:
	* src/sitetool-check.in:
	* src/sitetool-clean.in:
	* src/sitetool-digest.in:
	* src/sitetool-initialize.in:
	* src/sitetool-install.in:
	* src/sitetool-uninstall.in:
	* src/sitetool-validate.in:
	* src/sitetool.in:

	Fixed PATH_SEPARATOR substitution

2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* .gitignore:
	* configure.ac.in:
	* docs/Makefile.am:
	* docs/info/Makefile.am:
	* docs/info/sitetool.texi:
	* docs/man/Makefile.am:
	* libs/ruby/Makefile.am:

	Added missing files, removed info related targets

2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* configure.ac.in:
	* data/styles/default/Makefile.am:
	* libs/perl/Sitetool/Scheme.pm:
	* libs/scheme/Makefile.am:
	* libs/scheme/guile/Makefile.am:
	* src/filters/Makefile.am:

	Fixed build errors

2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* configure.ac.in:
	* libs/Makefile.am:
	* libs/perl/Sitetool/Filterchain.pm:
	* libs/shell/Makefile.am:

	Added ruby support

2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* docs/man/Makefile.am:
	* libs/perl/Sitetool/Autoconfig.pm.in:
	* libs/perl/Sitetool/Filterchain.pm:
	* src/sitetool-build.in:
	* src/sitetool-check.in:
	* src/sitetool-clean.in:
	* src/sitetool-digest.in:
	* src/sitetool-initialize.in:
	* src/sitetool-install.in:
	* src/sitetool-uninstall.in:
	* src/sitetool-validate.in:
	* src/sitetool.in:
	* test/site/Makefile.am:

	Renamed AS_* environment variables as SITETOOL_*

2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* Makefile.sub:
	* configure.ac.in:
	* libs/perl/Sitetool/Autoconfig.pm.in:
	* libs/perl/Sitetool/Scheme.pm:
	* test/site/test.in:

	Removed GUILE_LIB_DIR environment variable

2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* NEWS:

	make

2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* COPYING:
	* INSTALL:
	* Makefile.am:
	* THANKS:
	* data/styles/default/Makefile.am:
	* libs/scheme/guile/Makefile.am:

	Rearranged in order to let the distcheck target work

2008-01-05  Francesco Salvestrini  <francesco@sourceforge.net>

	* test/site/test.in:

	Rearranged test case

2008-01-04  Francesco Salvestrini  <francesco@sourceforge.net>

	* libs/perl/Sitetool/Digest.pm:
	* src/sitetool-digest.in:

	Fixed digest calculation

2008-01-04  Francesco Salvestrini  <francesco@sourceforge.net>

	* src/sitetool-digest.in:

	Updated and rearranged digest related code

2008-01-04  Francesco Salvestrini  <francesco@sourceforge.net>

	* libs/perl/Sitetool/Digest.pm:
	* src/sitetool-digest.in:

	Added remaining digests

2008-01-04  Francesco Salvestrini  <francesco@sourceforge.net>

	* .gitignore:
	* Makefile.sub:
	* configure.ac.in:
	* libs/perl/Sitetool/Autoconfig.pm.in:
	* libs/perl/Sitetool/Digest.pm:
	* src/Makefile.am:
	* src/sitetool-digest.in:
	* src/sitetool.in:

	Added digest computation
